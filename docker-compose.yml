services:
  opticaja:
    image: ghcr.io/daemon1s/opticaja:latest
    container_name: opticaja
    restart: unless-stopped
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
